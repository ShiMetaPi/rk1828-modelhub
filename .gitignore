# 模型大文件一律走 models/ 下载脚本，不进 git
*.rknn
*.weight
*.embed.bin
*.gguf
*.bin

# 本地环境
.claude/
.vscode/
__pycache__/
*.pyc

# 编译产物
vl_engine
vl_engine.*
depth_engine
yolo26_engine
build/

# 网卡限速等内部工具不放仓库
resume_push.py

# 性能测试数据（bench/）留本地，不进仓库
bench/

# 原始模型 / 官方 demo 源码留本地，不进仓库（模型已传 GitHub Release）
Qwen3tts/
talker/
rknn_Qwen3_ASR_demo/
rknn_Qwen3_TTS_demo/
spk_embed/

# 根目录 model*/ 是模型转换工作区（onnx / 测试图 / 中间产物），不进仓库
/model/
/model_pose/
/model_26/

# 语音样本 / 合成产物（含个人声音数据，不进公开仓库）
/*.wav
/*.mp3
tts/voices/
tts/gold/
asr/*.wav

# 本地模型镜像（PC 端给离线板子中转用，10GB，不进 git）
release_mirror/

# 拍摄分镜 / 板端内部分档（含个人 IP、环境信息，不进公开仓库）
docs_private/

# 板上实测留下的测试图 / 摄像头抓拍（含个人环境画面）
yolo26/model/
depth/demo_*
depth/frame_*
