# RK1828 modesetting 驱动偶发 crtc-99 flip timeout → Xorg 段错误（2026-09-21 抓到现场）。
# 关闭 page flip 掐灭该路径；chromium 全程 --disable-gpu 软渲染，关掉无性能损失。
Section "Device"
    Identifier "RK1828 noflip"
    Driver "modesetting"
    Option "PageFlip" "off"
EndSection
